{
  "name": "you-firestore-rules-tests",
  "private": true,
  "description": "Emulator tests for firestore.rules. Run: npm test",
  "scripts": {
    "test": "./run-tests.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "mocha": "^10.7.3"
  }
}

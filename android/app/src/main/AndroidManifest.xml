<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- CAMERA: taking a profile photo / photographing an ID card in-app. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- RECORD_AUDIO: voice journal entries. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
      NO media/storage permissions, by design. Picking an image from the gallery
      goes through Android's system Photo Picker (see main.dart:
      ImagePickerAndroid.useAndroidPhotoPicker), which requires no permission at
      all. Google Play's Photo and Video Permissions policy only allows
      READ_MEDIA_IMAGES / READ_MEDIA_VIDEO for apps whose core use case needs
      persistent access to shared media — ours are one-time picks, so declaring
      them got the app rejected. Do NOT re-add:
        READ_MEDIA_IMAGES, READ_MEDIA_VIDEO,
        READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!--
      AD_ID is REMOVED, deliberately. firebase_analytics merges it in
      transitively, but this app serves no ads and does no ad attribution.
      Leaving it declared forces an Advertising-ID disclosure in Play Data
      Safety for a mental-health app, for a capability we never use.
      Re-add it only if ad attribution ever becomes a real requirement.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <application
        android:label="You"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
